=== PROPOSED REGISTRY ===
  {'topic': 'OsaKano settings table (HKCU volumes): struct, indices, defaults, scale, registry-load loop', 'name': 'TotalVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Scale 0..100; 100=0x64=full master volume (code max-clamp = 0x64 at 0x4519CA). This is the table default (0x48C654).'}
  {'topic': 'OsaKano settings table (HKCU volumes): struct, indices, defaults, scale, registry-load loop', 'name': 'MusicVolume', 'value_dec': 100, 'scale_note': "REG_DWORD. Scale 0..100; 100=full. NOTE: game's built-in default is 50 (half) at 0x48C658; set 100 for maximum BGM. Use 50 to match factory default."}
  {'topic': 'OsaKano settings table (HKCU volumes): struct, indices, defaults, scale, registry-load loop', 'name': 'VoiceVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD. Scale 0..100; 100=0x64=full voice volume. Equals table default (0x48C65C).'}
  {'topic': 'OsaKano settings table (HKCU volumes): struct, indices, defaults, scale, registry-load loop', 'name': 'SEVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD. Scale 0..100; 100=0x64=full SE volume. Equals table default (0x48C660).'}
  {'topic': 'BGM (bgm01..bgm13.wav) playback path, volume gating, and the silence root-cause in osakano.exe', 'name': 'MusicVolume', 'value_dec': 100, 'scale_note': 'Scale is 0..100 (percent). Engine clamps to [0,100] via 0x43B880 (hi=0x64 pushed @0x43EEEA), then waveOut maps to 0..0xFFFF (full at 100). 100 = full volume. Factory-default template value @0x488D5C is also 100. Currently 0 in setup.reg -> silence. HKCU\\Software\\E-G-O\\OsaKano, REG_DWORD.'}
  {'topic': 'BGM (bgm01..bgm13.wav) playback path, volume gating, and the silence root-cause in osakano.exe', 'name': 'TotalVolume', 'value_dec': 100, 'scale_note': 'Master volume, settings idx31, scale 0..100. Factory template default is 50, but on the waveOut path TotalVolume setter 0x442430 calls CWave::SetVolume with the raw 0..100 value as a 16-bit level (so 50 -> 0x0032, near-silent) IF the device reports WAVECAPS_VOLUME; to avoid any attenuation set 100 (-> 0xFFFF). Currently 0 -> silence. REG_DWORD.'}
  {'topic': 'BGM (bgm01..bgm13.wav) playback path, volume gating, and the silence root-cause in osakano.exe', 'name': 'VoiceVolume', 'value_dec': 100, 'scale_note': "Voice channel, settings idx33, scale 0..100 (same clamp/0xFFFF mapping). Factory default 100. Currently 0 -> no voice. REG_DWORD. (Voice subsystem is sub04's area; included because the same loader/scale applies.)"}
  {'topic': 'BGM (bgm01..bgm13.wav) playback path, volume gating, and the silence root-cause in osakano.exe', 'name': 'SEVolume', 'value_dec': 100, 'scale_note': "Sound-effects channel, settings idx34, scale 0..100. Factory template default is 0, but 0 = silent SE; set 100 for audible effects. Currently 0. REG_DWORD. (SE is sub05's area; same loader/scale.)"}
  {'topic': 'sub03: BGM CD-DA (MCI) path vs waveOut+wav, and impact of the 0x580AD patch on music', 'name': 'MusicVolume', 'value_dec': 0, 'scale_note': 'CONFIRMED CURRENTLY 0 in setup.reg/HKCU -> this is what silences BGM. BGM is waveOut+wav (bgm01..bgm13.wav), gated by MusicVolume via waveOutSetVolume. Needs a non-zero value; exact scale/default per the volume settings-table near 0x48B144 (other sub-tasks). The patch did NOT cause this.'}
  {'topic': 'sub03: BGM CD-DA (MCI) path vs waveOut+wav, and impact of the 0x580AD patch on music', 'name': 'TotalVolume', 'value_dec': 0, 'scale_note': 'CONFIRMED CURRENTLY 0 in setup.reg. Master volume; at 0 it forces waveOutSetVolume(0) on all channels -> total silence. Set non-zero per settings-table scale.'}
  {'topic': 'sub03: BGM CD-DA (MCI) path vs waveOut+wav, and impact of the 0x580AD patch on music', 'name': 'VoiceVolume', 'value_dec': 0, 'scale_note': 'CONFIRMED CURRENTLY 0. Voice (voice.lax -> voice\\%s.wav) also plays through the same waveOut engine; 0 mutes voice. Set non-zero per settings-table scale.'}
  {'topic': 'sub03: BGM CD-DA (MCI) path vs waveOut+wav, and impact of the 0x580AD patch on music', 'name': 'SEVolume', 'value_dec': 0, 'scale_note': 'CONFIRMED CURRENTLY 0. SE (common.lax -> com\\se\\%s.wav) plays through the same waveOut engine; 0 mutes SE. Set non-zero per settings-table scale.'}
  {'topic': 'SE (sound-effect) playback: source = common.lax via com\\se\\*.wav; volume = SEVolume (idx 34) on a 0..100 scale -> millibel table @0x48CD20 (DirectSound/DirectShow mixer). SE on/off is gated by UseSE/MouseSE, NOT by SEVolume.', 'name': 'HKCU\\Software\\E-G-O\\OsaKano\\SEVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD on a 0..100 scale; 100 = full (0 mB / 0 dB), 0 = silent (-10000 mB). Converted via the logarithmic millibel table @0x48CD20 inside SetChannelVolume@0x452C00. In-EXE default is already 100, so this only matters if the registry currently holds an explicit 0/low value; set 100 to guarantee full SE.'}
  {'topic': 'SE (sound-effect) playback: source = common.lax via com\\se\\*.wav; volume = SEVolume (idx 34) on a 0..100 scale -> millibel table @0x48CD20 (DirectSound/DirectShow mixer). SE on/off is gated by UseSE/MouseSE, NOT by SEVolume.', 'name': 'HKCU\\Software\\E-G-O\\OsaKano\\UseSE', 'value_dec': 1, 'scale_note': 'REG_DWORD boolean gate (setting idx 8). 1=SE enabled, 0=all SE muted (hard skip at 0x40B334 and 0x441FDA). Default=1. Set/keep 1.'}
  {'topic': 'SE (sound-effect) playback: source = common.lax via com\\se\\*.wav; volume = SEVolume (idx 34) on a 0..100 scale -> millibel table @0x48CD20 (DirectSound/DirectShow mixer). SE on/off is gated by UseSE/MouseSE, NOT by SEVolume.', 'name': 'HKCU\\Software\\E-G-O\\OsaKano\\MouseSE', 'value_dec': 1, 'scale_note': 'REG_DWORD boolean gate (setting idx 10) for cursor/mouse SE only. 1=enabled, 0=mouse SE muted (hard skip at 0x441FF4, in addition to UseSE). Default=1. Set/keep 1.'}
  {'topic': 'SE (sound-effect) playback: source = common.lax via com\\se\\*.wav; volume = SEVolume (idx 34) on a 0..100 scale -> millibel table @0x48CD20 (DirectSound/DirectShow mixer). SE on/off is gated by UseSE/MouseSE, NOT by SEVolume.', 'name': 'HKCU\\Software\\E-G-O\\OsaKano\\TotalVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD 0..100 master volume (setting idx 31), same millibel table/scale; 100=full. Default=100. Listed because the silence is system-wide; ensure it is not 0.'}
  {'topic': 'Config UI volume sliders: scale (=100) and how the slider integer is persisted to registry (cross-check of the settings-table scale)', 'name': 'TotalVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Config UI slider scale = 100 (21 notches x5, capped 100); startup clamp hi=0x64 at 0x43B880. 100 = full (notch 20). Currently absent -> loaded as 0 (silence). Set to 0x64.'}
  {'topic': 'Config UI volume sliders: scale (=100) and how the slider integer is persisted to registry (cross-check of the settings-table scale)', 'name': 'MusicVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Slider value=notch*5 (idx32). Max=100. Set to 0x64 for full BGM volume; currently absent -> 0 -> no music.'}
  {'topic': 'Config UI volume sliders: scale (=100) and how the slider integer is persisted to registry (cross-check of the settings-table scale)', 'name': 'VoiceVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Slider value=notch*5, getVoiceVol does /5 (idx33). Max=100. Set to 0x64 for full voice; currently absent -> 0 -> no voice.'}
  {'topic': 'Config UI volume sliders: scale (=100) and how the slider integer is persisted to registry (cross-check of the settings-table scale)', 'name': 'SEVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Slider value=notch*5, getSEvol does /5 (idx34). Max=100. Set to 0x64 for full SE; currently absent -> 0 -> no sound effects.'}
  {'topic': 'TotalVolume master behavior + why absent volume regvals => total silence (DirectSound mB attenuation; settings array zero-init)', 'name': 'HKCU\\Software\\E-G-O\\OsaKano\\TotalVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD. Integer 0..100 master. 100 -> attenuation table[100]=0 mB (unity) on the DirectSound primary buffer. 0 -> -10000 mB (silent). This is the required master fix; without it the primary buffer is fully attenuated.'}
  {'topic': 'TotalVolume master behavior + why absent volume regvals => total silence (DirectSound mB attenuation; settings array zero-init)', 'name': 'HKCU\\Software\\E-G-O\\OsaKano\\MusicVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD 0..100, music secondary buffer (channel 8). 100 -> 0 mB unity. EGO template default was 50 (-602 mB, ~half); use 100 for clearly audible BGM. MUST be set: absent => 0 => music channel silent on its own, independent of TotalVolume.'}
  {'topic': 'TotalVolume master behavior + why absent volume regvals => total silence (DirectSound mB attenuation; settings array zero-init)', 'name': 'HKCU\\Software\\E-G-O\\OsaKano\\VoiceVolume', 'value_dec': 100, 'scale_note': "REG_DWORD 0..100, voice secondary buffer. 100 -> 0 mB unity (template default was also 100). MUST be set: absent => 0 => voice silent independent of TotalVolume. This is the direct fix for 'no voice'."}
  {'topic': 'TotalVolume master behavior + why absent volume regvals => total silence (DirectSound mB attenuation; settings array zero-init)', 'name': 'HKCU\\Software\\E-G-O\\OsaKano\\SEVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD 0..100, SE secondary buffer. 100 -> 0 mB unity (template default 100). MUST be set: absent => 0 => SE silent. (UseSE idx8 / UseMusic idx17 are separate enable flags handled by other subsystems; setting the four volumes is what restores audible levels.)'}
  {'topic': 'Effect of Music / MusicInstall / VoiceInstall flags on whether BGM/voice plays (osakano.exe, 2005 EGO VN)', 'name': 'Music', 'value_dec': 1, 'scale_note': 'HKCU\\Software\\E-G-O\\OsaKano\\Music (REG_DWORD). Engine semantics (switch at 0x441257): 0=CD-DA(MCI), 1=HDD wav via waveOut, 2=disabled, 3=HDD wav alt-path. Set to 1 so BGM uses the on-HDD bgm01..bgm13.wav through waveOut instead of the (patched-off) CD-DA path. CAVEAT: in the SHIPPED binary this registry value is on a dead branch (regModeFlag [esi+0x126C4] is never set non-zero), so writing it has NO effect unless regModeFlag is also enabled; if you cannot enable regModeFlag, use the byte patch at 0x3FCD3 instead. Not a volume; it is an enum {0,1,2,3}.'}
  {'topic': 'Effect of Music / MusicInstall / VoiceInstall flags on whether BGM/voice plays (osakano.exe, 2005 EGO VN)', 'name': 'MusicInstall', 'value_dec': 1, 'scale_note': "REG_DWORD boolean (0/1). Means 'music data installed to HDD' -> selects HDD source dir for music rather than CD. Recommended value 1 for an HDD-only Chinese release. NOTE: at runtime the live non-registry path already hard-codes MusicInstall=1 (store at 0x43FCC7), so this has no runtime effect in the shipped binary; set it only for correctness/parity if regModeFlag is ever enabled."}
  {'topic': 'Effect of Music / MusicInstall / VoiceInstall flags on whether BGM/voice plays (osakano.exe, 2005 EGO VN)', 'name': 'VoiceInstall', 'value_dec': 1, 'scale_note': "REG_DWORD boolean (0/1). Means 'voice data installed to HDD' -> opens voice.lax from InstallDir/VoicePath instead of the CD-ROM drive. Recommended 1. NOTE: at runtime the live path hard-codes VoiceInstall=1 (store at 0x43FCBE), so in the shipped binary this value is ignored; set only for parity / in case regModeFlag is enabled. It does NOT mute voice either way (it only chooses the source directory)."}
  {'topic': 'osakano.exe no-sound/no-voice root cause (absent volume registry values load as 0) + tested Core Audio peak-meter verifier', 'name': 'TotalVolume', 'value_dec': 100, 'scale_note': 'Master volume. 0..100 setting scale; 100 -> waveOutSetVolume 0xFFFF (full) per channel, 0 -> silent. Currently ABSENT -> loads as 0 -> master silence. Create as REG_DWORD = 100 (decimal) = 0x00000064. Path: HKCU\\Software\\E-G-O\\OsaKano. Original baked template value was also 100.'}
  {'topic': 'osakano.exe no-sound/no-voice root cause (absent volume registry values load as 0) + tested Core Audio peak-meter verifier', 'name': 'MusicVolume', 'value_dec': 100, 'scale_note': "BGM (wav via waveOut) volume, 0..100. ABSENT -> 0 -> no music. REG_DWORD = 100 (0x64) recommended for verification (game's own template default was 50; 100 = max audibility, user can lower in-game options)."}
  {'topic': 'osakano.exe no-sound/no-voice root cause (absent volume registry values load as 0) + tested Core Audio peak-meter verifier', 'name': 'VoiceVolume', 'value_dec': 100, 'scale_note': 'Voice (voice.lax via waveOut) volume, 0..100. ABSENT -> 0 -> no voice. REG_DWORD = 100 (0x64). Original baked template default was 100; runtime shadow had 50. Use 100 to confirm audibility.'}
  {'topic': 'osakano.exe no-sound/no-voice root cause (absent volume registry values load as 0) + tested Core Audio peak-meter verifier', 'name': 'SEVolume', 'value_dec': 100, 'scale_note': 'Sound-effects (common.lax via waveOut) volume, 0..100. ABSENT -> 0 -> no SE. REG_DWORD = 100 (0x64). Original baked template default was 100.'}
=== PROPOSED PATCHES ===
  {"topic": "Effect of Music / MusicInstall / VoiceInstall flags on whether BGM/voice plays (osakano.exe, 2005 EGO VN)", "file_offset_hex": "0x3FCD3", "orig_bytes_hex": "8B86BC260100", "new_bytes_hex": "B801000000 90", "why": "At the LIVE runtime config path (non-registry branch, reached because regModeFlag is always 0), this instruction is 'mov eax,[esi+0x126BC]' which loads the Music-source field (always 0 because that field is never written), after which 'test eax,eax / jle 0x43FCE3' skips storing it so Music ([esi+0x238]) stays 0 -> CD-DA -> silent. Replacing it with 'mov eax,1' (B8 01 00 00 00) + NOP (90) makes eax=1 so the following test/jle falls through and 'mov [esi+0x238],eax' at 0x43FCDD stores Music=1, forcing the BGM dispatcher (0x441257) into the HDD-wav + waveOut branch. This is the certain music fix and does not depend on regModeFlag. Original 6 bytes confirmed read from file at off 0x3FCD3 = 8B 86 BC 26 01 00. (Length-preserving: 6 bytes -> 5-byte mov + 1 NOP.) NOTE: enables the BGM code path only; audible level still requires the *Volume registry values / volume subsystem, and voice is a separate path.)"}
=== SUBSYSTEM FINDINGS ===
### TOPIC: OsaKano settings table (HKCU volumes): struct, indices, defaults, scale, registry-load loop
   - The 4 volume settings ARE part of one 38-entry table loaded by a single RegQueryValueExA loop. Table = two PARALLEL arrays: name pointers at VA 0x48C510 (file 0x8C510) and default DWORDs at VA 0x48C5D8 (file 0x8C5D8), indexed in lockstep, 4-byte LE each. NOT an interleaved {name,default} struct.
   - Volume indices and DEFAULTS (byte-verified): idx31 TotalVolume=100 (0x64@0x48C654), idx32 MusicVolume=50 (0x32@0x48C658), idx33 VoiceVolume=100 (0x64@0x48C65C), idx34 SEVolume=100 (0x64@0x48C660).
   - Scale is 0..100 percent: 100 (0x64) = full/max, 0 = silent. Proven by the max-clamp 'mov edi,0x64' at 0x4519CA in setVolume(this,vol) (0x4519B0) and by Music's default being exactly half (50) of the others (100). No min/max data array exists; bounds are code-enforced.
   - Registry value type is REG_DWORD, 4 bytes, little-endian (reader 0x4619F0 uses mov eax,4 then RegQueryValueExA at 0x461A74; IAT 0x522884).
   - When the HKCU value is ABSENT in REGISTRY mode, the loop KEEPS the pre-seeded table default (reader leaves the output buffer untouched on RegQueryValueExA failure). So absent volume values alone yield Total=100/Music=50/Voice=100/SE=100 = audible, NOT silence. Silence from this table would require the values being present-and-0, or the reader runni
   - If the diagnosis is 'volume values currently 0 in registry', the fix is to write each as REG_DWORD with the value below; if they are merely absent, the engine already defaults them to audible, so the silence root cause is elsewhere (CD-DA/MCI music path or the voice/SE playback gates handled by sibling tasks).
   REG: [{'name': 'TotalVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Scale 0..100; 100=0x64=full master volume (code max-clamp = 0x64 at 0x4519CA). This is the table default (0x48C654).'}, {'name': 'MusicVolume', 'value_dec': 100, 'scale_note': "REG_DWORD. Scale 0..100; 100=full. NOTE: game's built-in default is 50 (half) at 0x48C658; set 100 for maximum BGM. Use 50 to match factory default."}, {'name': 'VoiceVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD. Scale 0..100; 100=0x64=full voice volume. Equals table default (0x48C65C).'}, {'name': 'SEVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD. Scale 0..100; 100=0x64=full SE volume. Equals table default (0x48C660).'}]

### TOPIC: sub01 volume scale and silence root cause
   - Scale is 0..100; defaults Total=100 Music=50 Voice=100 SE=100; 0 silent, 100 full.
   - Missing or 0 registry volume yields 0 in memory and full silence via DirectSound -10000 mB.
   - Fix: write REG_DWORD TotalVolume=100 MusicVolume=100 VoiceVolume=100 SEVolume=100 under HKCU Software E-G-O OsaKano.

### TOPIC: BGM (bgm01..bgm13.wav) playback path, volume gating, and the silence root-cause in osakano.exe
   - SILENCE ROOT CAUSE (BGM/voice/SE): the 4 *Volume registry values are 0 (setup.reg writes dword:00000000; 启动游戏.bat never writes them). The settings loader @0x43FF00 memsets runtime values to 0 then RegQueryValueExA(0x4619F0); a missing/failed read leaves the slot 0. The factory-default template @0x488D5C (TotalVolume=50, MusicVolume=100, VoiceVolume
   - VOLUME SCALE IS 0..100 (percent). The clamp helper 0x43B880 is invoked for every channel with lo=0, hi=0x64 (100) (hi pushed @0x43EEEA). getSetting(idx) returns the raw registry value; clamp to 0..100 then handed to the channel setter.
   - MusicVolume = settings index 32 (name 'MusicVolume' @0x48B160; runtime slot mainObj+0x5C+32*4). Verified by reading the live name-pointer array at 0x48C510. TotalVolume=idx31, VoiceVolume=idx33, SEVolume=idx34.
   - waveOut full-scale = 0xFFFF per channel. The music volume-fade proc 0x452290 ramps toward 0xFFFF (push 0xFFFF; clamp(0,0xFFFF)) and calls CWave::SetVolume(0x452170)->waveOutSetVolume([0x522F18]). So MusicVolume 100 -> 0xFFFF (full), 0 -> 0x0000 (silent). Linear mapping target ~= round(pct/100 * 0xFFFF).
   - BGM source = bgmNN.wav on HDD via waveOut (NOT CD-DA, NOT DirectSound). Template 'bgm%02d.wav' @0x48B810 -> cfg slot [0x4C44DC]. Per-track play: scenario opcode @0x40B3xx (obj @0x4B1F58) -> BeginMusicStream(0x441810) -> SndMgr::PlayA(0x451750) -> CWave waveOutOpen/PrepareHeader/Write (0x451C1E/0x451E83/0x451EAE).
   - MusicInstall=1 DOES enable the wav path: MusicDispatch(0x441230) @0x4412CD checks MusicInstall (mainObj+0x250); if !=0 it uses the installed path mainObj+0x12584 (InstallDir+bgmNN.wav) directly; if ==0 it prepends the CD-ROM drive. MusicInstall=1 is already set, so the HDD wav path is selected.
   REG: [{'name': 'MusicVolume', 'value_dec': 100, 'scale_note': 'Scale is 0..100 (percent). Engine clamps to [0,100] via 0x43B880 (hi=0x64 pushed @0x43EEEA), then waveOut maps to 0..0xFFFF (full at 100). 100 = full volume. Factory-default template value @0x488D5C is also 100. Currently 0 in setup.reg -> silence. HKCU\\Software\\E-G-O\\OsaKano, REG_DWORD.'}, {'name': 'TotalVolume', 'value_dec': 100, 'scale_note': 'Master volume, settings idx31, scale 0..100. Factory template default is 50, but on the waveOut path TotalVolume setter 0x442430 calls CWave::SetVolume with the raw 0..100 value as a 16-bit level (so 50 -> 0x0032, near-silent) IF the device reports WAVECAPS_VOLUME; to avoid any attenuation set 100 (-> 0xFFFF). Currently 0 -> silence. REG_DWORD.'}, {'name': 'VoiceVolume', 'value_dec': 100, 'scale_note': "Voice channel, settings idx33, scale 0..100 (same clamp/0xFFFF mapping). Factory default 100. Currently 0 -> no voice. REG_DWORD. (Voice subsystem is sub04's area; included because the same loader/scale applies.)"}, {'name': 'SEVolume', 'value_dec': 100, 'scale_note': "Sound-effects channel, settings idx34, scale 0..100. Factory template default is 0, but 0 = silent SE; set 100 for audible effects. Currently 0. REG_DWORD. (SE is sub05's area; same loader/scale.)"}]

### TOPIC: sub03: BGM CD-DA (MCI) path vs waveOut+wav, and impact of the 0x580AD patch on music
   - DEFINITIVE: In-game BGM (and voice and SE) is played via Win32 waveOut + .wav files on the HDD, NOT via CD-DA/MCI. The bgm%02d.wav template (VA 0x48B810) feeds wav player 0x451D10, which validates RIFF/WAVE/data magics and calls waveOutWrite [0x522F30]. The on-disk bgm01..bgm13.wav are genuine RIFF/WAVE 22050Hz/16-bit PCM matching the engine's WAVE
   - The 0x580AD patch (file_off 0x580AD / VA 0x4580AD) is inside the MCI/CD-DA wrapper 0x458080 and only runs on the mciSendCommandA ERROR path. It did NOT and cannot mute BGM/voice/SE, because those never use MCI. The wrapper 0x458080 is called exclusively from the 0x457xxx CD-DA subsystem (15 call sites, all in 0x457940..0x458120); the wave engine ha
   - Patch semantics: ORIG 74 4C = 'je 0x4580FB' (only suppressed prompt when CD-state==playing) was changed to EB 53 = 'jmp 0x458102' (always go straight to 'return 0', skipping the 'insert CD' MessageBox + retry). This is a CD-prompt suppression patch consistent with the other CD/OS-warning removals. On MCI SUCCESS the wrapper already returns 1 at 0x4
   - Therefore the patch must NOT be reverted to fix sound: reverting 0x580AD would only re-enable the legacy 'insert CD' dialog when the optional CD-DA path errors — it would restore ZERO audio. No redo/revert is warranted for this patch.
   - ACTUAL ROOT CAUSE of silence (confirmed in E:\game\jp\setup.reg, which was applied to HKCU): TotalVolume, MusicVolume, VoiceVolume and SEVolume are all written as dword:00000000. With the volumes at 0, the engine calls waveOutSetVolume(0) and every wav channel is muted -> no sound, no voice. Music=1 and MusicInstall=1 are correct (BGM enabled, play
   REG: [{'name': 'MusicVolume', 'value_dec': 0, 'scale_note': 'CONFIRMED CURRENTLY 0 in setup.reg/HKCU -> this is what silences BGM. BGM is waveOut+wav (bgm01..bgm13.wav), gated by MusicVolume via waveOutSetVolume. Needs a non-zero value; exact scale/default per the volume settings-table near 0x48B144 (other sub-tasks). The patch did NOT cause this.'}, {'name': 'TotalVolume', 'value_dec': 0, 'scale_note': 'CONFIRMED CURRENTLY 0 in setup.reg. Master volume; at 0 it forces waveOutSetVolume(0) on all channels -> total silence. Set non-zero per settings-table scale.'}, {'name': 'VoiceVolume', 'value_dec': 0, 'scale_note': 'CONFIRMED CURRENTLY 0. Voice (voice.lax -> voice\\%s.wav) also plays through the same waveOut engine; 0 mutes voice. Set non-zero per settings-table scale.'}, {'name': 'SEVolume', 'value_dec': 0, 'scale_note': 'CONFIRMED CURRENTLY 0. SE (common.lax -> com\\se\\%s.wav) plays through the same waveOut engine; 0 mutes SE. Set non-zero per settings-table scale.'}]

### TOPIC: SE (sound-effect) playback: source = common.lax via com\se\*.wav; volume = SEVolume (idx 34) on a 0.
   - SEVolume is settings index 34 (0x22). Its in-EXE DEFAULT is 100 (dword @0x48C660 = 0x64), on a 0..100 scale where 100 = full volume. Verified bytes: name[34]@0x48C598=0x0048B144('SEVolume'); default[34]@0x48C660=0x64.
   - Because LoadOneSetting/GetSetting keep the in-EXE default when the registry value is ABSENT (RegQueryValueExA failure leaves settings[i]=default), an empty/missing HKCU SEVolume does NOT yield 0 -- it yields 100 (full). So 'missing SEVolume' alone does NOT explain silence. Silence from SEVolume requires the registry to explicitly contain SEVolume=0
   - The 0..100 -> device conversion is a logarithmic millibel lookup table @0x48CD20 (DirectSound DSBVOLUME range -10000..0), NOT a linear *0xFFFF/100 and NOT waveOutSetVolume. SetChannelVolume@0x452C00: vol==0 -> -10000 mB (silent); vol>=100 -> 0 mB (full); else VOLTABLE[vol]. Verified: vol50=-602 mB (half amplitude), vol100=0 mB.
   - SE on/off is gated by UseSE (idx 8, default 1) on BOTH the scripted path (0x40B334) and the mouse/cursor path (0x441FDA); cursor SE is ADDITIONALLY gated by MouseSE (idx 10, default 1). SEVolume is NOT a gate -- it only attenuates. Therefore: if HKCU has UseSE=0 (or MouseSE=0 for cursor sounds), SE is muted regardless of SEVolume. Confirm these two
   - HARD DEPENDENCY / co-cause: SetChannelVolume@0x452C00 first checks the DirectSound/DirectShow mixer singleton pointer [0x503e20]; if it is NULL (mixer never created) the function returns 0 and the volume is never set. The SE/sound mixer is DirectSound (DSOUND.dll ord1 = DirectSoundCreate) + DirectShow (ole32.CoCreateInstance), object @0x503e20 (BSS
   - SE source is the archive common.lax (global ptr 0x4C44E4, literal 0x48B7E4); SE member WAVs are addressed via com\se\%s.wav (0x4C44C8) and com\se\sys\s%s.wav (0x4C44CC). SE plays on SndObj/mixer channels 13,14,15 (ApplySE@0x441A90).
   REG: [{'name': 'HKCU\\Software\\E-G-O\\OsaKano\\SEVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD on a 0..100 scale; 100 = full (0 mB / 0 dB), 0 = silent (-10000 mB). Converted via the logarithmic millibel table @0x48CD20 inside SetChannelVolume@0x452C00. In-EXE default is already 100, so this only matters if the registry currently holds an explicit 0/low value; set 100 to guarantee full SE.'}, {'name': 'HKCU\\Software\\E-G-O\\OsaKano\\UseSE', 'value_dec': 1, 'scale_note': 'REG_DWORD boolean gate (setting idx 8). 1=SE enabled, 0=all SE muted (hard skip at 0x40B334 and 0x441FDA). Default=1. Set/keep 1.'}, {'name': 'HKCU\\Software\\E-G-O\\OsaKano\\MouseSE', 'value_dec': 1, 'scale_note': 'REG_DWORD boolean gate (setting idx 10) for cursor/mouse SE only. 1=enabled, 0=mouse SE muted (hard skip at 0x441FF4, in addition to UseSE). Default=1. Set/keep 1.'}, {'name': 'HKCU\\Software\\E-G-O\\OsaKano\\TotalVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD 0..100 master volume (setting idx 31), same millibel table/scale; 100=full. Default=100. Listed because the silence is system-wide; ensure it is not 0.'}]

### TOPIC: Audio device init (waveOutGetNumDevs/waveOutOpen) and the global audio-enable flag
   - DEVICE-INIT IS NOT THE CAUSE OF THE SILENCE. The waveOut device-open path (sub_451A90 @0x451A90) is byte-for-byte ORIGINAL (none of the applied patches 0x1AD86/0x3F873/0x3F87D/0x46EC6/0x580AD/0x8CB10 fall inside the audio engine code range file 0x51660-0x52400). It is robust: on any normal Windows PC waveOutGetNumDevs()>=1 and waveOutOpen succeeds,
   - The ONLY way device-init globally disables audio is if waveOutGetNumDevs()==0 OR waveOutOpen fails for every device/format. That leaves [obj+0x76]=0 (silent) and merely logs 'WAVE无法使用(CODE:n)' (str 0x48CD00) via the in-game text printer - it does NOT block. This is a real 'no device -> disable audio' path but it is NOT normally hit; it is not the b
   - THE GLOBAL AUDIO-ENABLE FLAG is the per-object field [audioObj+0x76], absolute VA 0x4C13D0 (game static 0x4B1F58 + embed 0xF402 + 0x76). Set to 1 exactly once, at 0x4516C4, only on a successful waveOutOpen during configure (sub_451660). There is NO independent .data boolean for 'sound on/off' separate from this and the volume values.
   - The patch at file 0x580AD (jmp 0x458102, bytes 'eb 53') sits in the mciSendCommandA/CD-DA wrapper sub_458080 (drives 'mci\\cdaudio'). Since BGM is bgm%02d.wav played via waveOut (filename table 0x48B810), this CD-DA patch is IRRELEVANT to bgmXX.wav music - it did NOT kill the music. It only short-circuits the CD-DA error/retry/MessageBox branch.
   - Because device-init normally sets [obj+0x76]=1 and [obj+0x45]=1, waveOutSetVolume @0x4521A7 IS reached - it just gets handed the cached volume DWORD (0xLLLLRRRR). A volume of 0 (from the absent/0 registry MusicVolume/VoiceVolume/SEVolume/TotalVolume) is full per-channel silence at the device. So the silence is consistent with the registry-volume ro

### TOPIC: Config UI volume sliders: scale (=100) and how the slider integer is persisted to registry (cross-ch
   - Settings table located: NULL-terminated name-pointer array at VA 0x48C510 (file off 0x8C510), 50 entries used by the registry loop; duplicate copy at VA 0x488C90 (off 0x88C90). Pure pointer array, stride 4, NO embedded defaults. Volume entries: idx31=TotalVolume, idx32=MusicVolume, idx33=VoiceVolume, idx34=SEVolume (base 0x48C510).
   - Config value storage: each setting is a DWORD at config_obj+0x5c+idx*4, mirrored to global array 0x48C5D8+idx*4. The 4 volumes live at config+0xD8/0xDC/0xE0/0xE4 (idx 31..34).
   - Config UI slider scale = 100. Slider has 21 notches (0..20); on change value=notch*5 capped at 100 (Voice apply 0x41B360, SE apply 0x41B38B: cmp esi,0x14 / mov eax,0x64 / lea eax,[esi+esi*4]). On display value is divided by 5 to recover the notch (getVoice 0x41B5F2, getSE 0x41B61F use imul 0x66666667 = /5). MAX slider value (=scale) is therefore 10
   - Independent confirmation of scale=100: the startup read-apply routine (0x43EF14/0x43EF36/0x43EF57/0x43EF78) reads each volume via cfgGetByIdx(idx 0x1F..0x22) then calls clamp(value,0,100) at 0x43B880 (hi operand = 0x64). All four volumes are hard-clamped to [0,100].
   - Save path verified byte-for-byte: apply* -> cfgSetByIdx(0x4402c0) stores to config+0x5c+idx*4 then calls SetInt(0x461870) at 0x440386, which is RegSetValueExA(hKey, name, 0, REG_DWORD=4, &value, 4). Root = HKEY_CURRENT_USER (0x80000001), subkey 'Software\E-G-O\OsaKano' (VA 0x48B8F0). Value names used: TotalVolume/MusicVolume/VoiceVolume/SEVolume (s
   - Silence root cause: registry LOAD loop at 0x43FF9C is preceded by memset(config+0x5c, 0x00, 0xC8) at 0x43FF8B-0x43FF92 (0x4432C0 = memset; fill byte ebx=0). GetIntDef (0x4619F0) on the registry path does NOT write a default when RegQueryValueExA fails, so an ABSENT *Volume value leaves the slot = 0. With the 4 *Volume REG_DWORDs absent, all volumes
   REG: [{'name': 'TotalVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Config UI slider scale = 100 (21 notches x5, capped 100); startup clamp hi=0x64 at 0x43B880. 100 = full (notch 20). Currently absent -> loaded as 0 (silence). Set to 0x64.'}, {'name': 'MusicVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Slider value=notch*5 (idx32). Max=100. Set to 0x64 for full BGM volume; currently absent -> 0 -> no music.'}, {'name': 'VoiceVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Slider value=notch*5, getVoiceVol does /5 (idx33). Max=100. Set to 0x64 for full voice; currently absent -> 0 -> no voice.'}, {'name': 'SEVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD under HKCU\\Software\\E-G-O\\OsaKano. Slider value=notch*5, getSEvol does /5 (idx34). Max=100. Set to 0x64 for full SE; currently absent -> 0 -> no sound effects.'}]

### TOPIC: TotalVolume master behavior + why absent volume regvals => total silence (DirectSound mB attenuation
   - TotalVolume is NOT a software multiplier/limiter of Music/Voice/SE. It is applied (channel_id = -1) ONLY to the DirectSound PRIMARY buffer (0x503e24) via IDirectSoundBuffer::SetVolume(mB); Music(ch 8)/Voice/SE are applied to their own SECONDARY buffers independently. The secondary-channel branch in the mixer 0x452c00 never reads TotalVolume or 0x48
   - TotalVolume nonetheless acts as a hardware master via the primary buffer: TotalVolume=0 sets the primary buffer to -10000 mB. On the normal DirectSound path the primary-buffer volume attenuates the final mixed output, so TotalVolume=0 effectively silences everything regardless of per-channel values. So 'TotalVolume=0 => everything silent' is TRUE i
   - The actual silence is OVER-DETERMINED and does NOT depend on the TotalVolume-master question: the live settings array obj+0x5c is memset to 0 at load (VA 0x43FF7A: memset(obj+0x5c,200,0)), and absent registry values are left at 0 because the load helper (0x4619f0/0x440190) writes a slot only when the value exists. The compiled defaults 100/50/100/1
   - Mixer mapping value 0 -> -10000 mB is unconditional and applies to every channel: at 0x452C4F, test ebx,ebx; jne; mov ecx,0xFFFFD8F0 (=-10000) BEFORE the channel dispatch. Verified attenuation table 0x48cd20: [0]=-10000, [50]=-602, [100]=0. Hence value 0 on Music/Voice/SE secondary buffers also = silence on those channels by themselves.
   - NET CAUSE OF 'no sound, no voice': all four volume DWORDs are absent from HKCU\Software\E-G-O\OsaKano, so each resolves to 0 -> -10000 mB -> every DirectSound buffer (primary + music + voice + SE) is fully attenuated. This is independent of the BGM/CD-DA patch question.
   - Scale confirmed = integer 0..100 (clamp min=0 max=0x64 at 0x43b880; options slider = step*5 capped at 100; mB table indexed 0..100). REG type = REG_DWORD (type 4, size 4). EGO-intended template defaults were TotalVolume=100, MusicVolume=50, VoiceVolume=100, SEVolume=100.
   REG: [{'name': 'HKCU\\Software\\E-G-O\\OsaKano\\TotalVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD. Integer 0..100 master. 100 -> attenuation table[100]=0 mB (unity) on the DirectSound primary buffer. 0 -> -10000 mB (silent). This is the required master fix; without it the primary buffer is fully attenuated.'}, {'name': 'HKCU\\Software\\E-G-O\\OsaKano\\MusicVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD 0..100, music secondary buffer (channel 8). 100 -> 0 mB unity. EGO template default was 50 (-602 mB, ~half); use 100 for clearly audible BGM. MUST be set: absent => 0 => music channel silent on its own, independent of TotalVolume.'}, {'name': 'HKCU\\Software\\E-G-O\\OsaKano\\VoiceVolume', 'value_dec': 100, 'scale_note': "REG_DWORD 0..100, voice secondary buffer. 100 -> 0 mB unity (template default was also 100). MUST be set: absent => 0 => voice silent independent of TotalVolume. This is the direct fix for 'no voice'."}, {'name': 'HKCU\\Software\\E-G-O\\OsaKano\\SEVolume', 'value_dec': 100, 'scale_note': 'REG_DWORD 0..100, SE secondary buffer. 100 -> 0 mB unity (template default 100). MUST be set: absent => 0 => SE silent. (UseSE idx8 / UseMusic idx17 are separate enable flags handled by other subsystems; setting the four volumes is what restores audible levels.)'}]

### TOPIC: Effect of Music / MusicInstall / VoiceInstall flags on whether BGM/voice plays (osakano.exe, 2005 EG
   - BGM source is chosen by the 'Music' flag at config-object offset +0x238 (registry value name 'Music' = string 0x48CA48, NOT 0x48B237). Music==0 => CD-DA via WINMM mixer + mciSendCommandA; Music==1 or 3 => HDD bgmNN.wav via waveOut. Music==2 => BGM hard-disabled (returns 0). Verified by the 4-way switch at 0x441257 and the parallel 'if Music==0 -> C
   - Because the localization already neutralized the CD-DA MCI wrapper (patch at 0x580AD), the only way to get audible BGM from the on-HDD bgm01..bgm13.wav is to make the engine take the waveOut branch, i.e. Music must be 1 (HDD install) or 3. With Music==0 the engine routes BGM to the now-dead CD-DA path => silence. (This is the music half of the no-s
   - 'MusicInstall' and 'VoiceInstall' do NOT enable/disable audio. They (and 'GrInstall') only select the HDD-vs-CD source directory for the corresponding .lax archive (voice.lax / system archive). Confirmed at archive-mounter 0x43F1D0 and file->archive resolver 0x440C40 (VoiceInstall returned at 0x440CF0, GrInstall at 0x440CBE). So they are not the ca
   - DEAD-CODE finding: the registry reads of 'Music' (0x43FAC5), 'VoiceInstall' (0x43FADE), 'MusicInstall' (0x43FB00), 'GrInstall'/'Gr' are gated by regModeFlag [esi+0x126C4], which is only ever written =0 (constructor 0x402830) and never set non-zero anywhere in .text (verified by direct-disp and lea-alias scans). The config loader therefore always ta
   - 'UseMusic' (registry value name at 0x48B234, options-table index 17, slot [config+0xA0]) is the user BGM on/off preference, but in the config object it is NOT used to gate BGM start; in the code I see it is consumed only as an options-menu slider coordinate (0x438FFB/0x439031/0x439328...). The task's 'Music 0x48B237' is the unused tail of 'UseMusic
   - Because the registry path is dead, the certain fix for the music half is a byte patch that forces Music into the waveOut branch at the live assignment site 0x43FCD3 (file off 0x3FCD3). Setting Music=1 there makes the BGM dispatcher take the HDD-wav/waveOut branch unconditionally. (Volume still depends on the absent *Volume registry values handled b
   REG: [{'name': 'Music', 'value_dec': 1, 'scale_note': 'HKCU\\Software\\E-G-O\\OsaKano\\Music (REG_DWORD). Engine semantics (switch at 0x441257): 0=CD-DA(MCI), 1=HDD wav via waveOut, 2=disabled, 3=HDD wav alt-path. Set to 1 so BGM uses the on-HDD bgm01..bgm13.wav through waveOut instead of the (patched-off) CD-DA path. CAVEAT: in the SHIPPED binary this registry value is on a dead branch (regModeFlag [esi+0x126C4] is never set non-zero), so writing it has NO effect unless regModeFlag is also enabled; if you cannot enable regModeFlag, use the byte patch at 0x3FCD3 instead. Not a volume; it is an enum {0,1,2,3}.'}, {'name': 'MusicInstall', 'value_dec': 1, 'scale_note': "REG_DWORD boolean (0/1). Means 'music data installed to HDD' -> selects HDD source dir for music rather than CD. Recommended value 1 for an HDD-only Chinese release. NOTE: at runtime the live non-registry path already hard-codes MusicInstall=1 (store at 0x43FCC7), so this has no runtime effect in the shipped binary; set it only for correctness/parity if regModeFlag is ever enabled."}, {'name': 'VoiceInstall', 'value_dec': 1, 'scale_note': "REG_DWORD boolean (0/1). Means 'voice data installed to HDD' -> opens voice.lax from InstallDir/VoicePath instead of the CD-ROM drive. Recommended 1. NOTE: at runtime the live path hard-codes VoiceInstall=1 (store at 0x43FCBE), so in the shipped binary this value is ignored; set only for parity / in case regModeFlag is enabled. It does NOT mute voice either way (it only chooses the source directory)."}]
   PATCH: [{"file_offset_hex": "0x3FCD3", "orig_bytes_hex": "8B86BC260100", "new_bytes_hex": "B801000000 90", "why": "At the LIVE runtime config path (non-registry branch, reached because regModeFlag is always 0), this instruction is 'mov eax,[esi+0x126BC]' which loads the Music-source field (always 0 because that field is never written), after which 'test eax,eax / jle 0x43FCE3' skips storing it so Music ([esi+0x238]) stays 0 -> CD-DA -> silent. Replacing it with 'mov eax,1' (B8 01 00 00 00) + NOP (90) m

### TOPIC: sub10 — Audit of applied patches for audio impact (osakano.exe)
   - PATCH 0x580AD is NOT the cause of silence and does NOT kill BGM. BGM uses waveOut+'bgm%02d.wav', a completely separate code path from this MCI/CD-DA wrapper (sub 0x458080 which calls mciSendCommandA for device 'mci\cdaudio'). Verified the patch tail: orig 74 4c = je 0x4580FB (mov byte[0x50CF00],0; xor eax,eax; ret 4); new eb 53 = jmp 0x458102 (xor 
   - PATCH 0x1AD86 (voice verify, func 0x41AD50) is audio-NEUTRAL / mildly enabling. orig f7 d8 1b c0 5f f7 d8 5e c3 = (neg eax;sbb eax,eax;...;neg eax) i.e. return bool(result of call 0x440B20) -> 'does named voice entry resolve'. new b8 01 00 00 00 5f 5e c3 90 = mov eax,1;pop edi;pop esi;ret;nop -> always return 1 ('present'). The call 0x440B20 (path 
   - RENDER/CHARSET/FONT patches do NOT touch the audio call graph. Verified bytes: 0x8CB10/14/18/1C 01->02 (.data DBCS classify table @VA0x48CB10), 0x46EC6 72->EB (DBCS byte-order branch @0x446EC6), 0x5C507 9F->FE (IsSJISLead widen @0x45C507), 0x61DC1 80->86 and 0x61EC7 80->86 (lfCharSet SHIFT_JIS->GB2312 in CreateFont setup @0x461DC1/0x461EC7), plus f
   - OS-SKIP patches 0x3F873 (74 0A->EB 30) and 0x3F87D (75->EB) are audio-irrelevant. Confirmed context: they convert je/jne into jmp 0x43F8A5, skipping a MessageBoxA 'unsupported OS' warning (title 0x48BA1C='错误', call [0x522DB8]=MessageBoxA) and jumping straight to continue-init (call 0x443050). No waveOut/MCI in this path.
   - OVERALL: full binary diff against osakano.exe.jp.bak = exactly 18 .text bytes (the 8 documented patches) + 3599 .data bytes (Chinese text). There are NO undocumented code patches. NONE of the 8 patches mute or disable the waveOut audio engine. The actual silence is a DATA problem: HKCU\Software\E-G-O\OsaKano TotalVolume/MusicVolume/VoiceVolume/SEVo

### TOPIC: osakano.exe no-sound/no-voice root cause (absent volume registry values load as 0) + tested Core Aud
   - ROOT CAUSE: the four HKCU\Software\E-G-O\OsaKano volume values (TotalVolume/MusicVolume/VoiceVolume/SEVolume) are absent; the settings loader at VA 0x43FF00 memset()s the value array (engine+0x5C, 0xC8 bytes) to 0 BEFORE the registry read, and the per-setting loader 0x4619F0 returns without writing the destination when RegQueryValueExA fails (value
   - Settings table located: 37-entry name-pointer array. LIVE copy = Array B base VA 0x48C510 (file off 0x8C510); the load loop iterates it (mov esi,0x48C510 at 0x43FF97; cmp esi,0x48C5A8). A second identical Array A at VA 0x488C90 has ZERO code references (dead/template). Volume indices: TotalVolume=30, MusicVolume=31, VoiceVolume=32, SEVolume=33; sto
   - There is NO default-application: the baked default-value template at VA 0x488D5C (TotalVolume=100, MusicVolume=50, VoiceVolume=100, SEVolume=100) is never referenced in the entire image (verified by full-file 4-byte pointer scan) and is never copied into the live values; only registry values fill them.
   - waveOutSetVolume scale CONFIRMED at the only real call site (VA 0x4521A7, IAT [0x522F18]): the DWORD is built as ((left&0xFFFF)<<16)|(right&0xFFFF) at 0x452184-0x4521A2; 0xFFFF=full per channel, 0=silent. Settings are 0..100 (config max=100, 0x64 constant), mapped so 100->0xFFFF and 0->0. The SetVolume method is 0x452170; it is a no-op if the devic
   - Registry value TYPE is REG_DWORD (4 bytes): loader 0x4619F0 takes the [this+0x14]==0 branch -> mov eax,4 (cbData=4) -> RegQueryValueExA reads exactly 4 bytes (0x461A55..0x461A74). The REG_SZ/itoa branch (0x461A01, save path 0x461790 uses type=1) is only for string-typed settings, not the volumes.
   - APPLIED PATCH 0x580AD (VA 0x4580AD, EB 53 jmp 0x458102) is INNOCENT: it only skips the CD-DA/mciSendCommandA error MessageBox path (call [0x50CF08] + push 0x1035 prompt). BGM is wav-via-waveOut, a separate source, so it does not affect waveOut SE/voice/wav-BGM. It did NOT mute audio.
   REG: [{'name': 'TotalVolume', 'value_dec': 100, 'scale_note': 'Master volume. 0..100 setting scale; 100 -> waveOutSetVolume 0xFFFF (full) per channel, 0 -> silent. Currently ABSENT -> loads as 0 -> master silence. Create as REG_DWORD = 100 (decimal) = 0x00000064. Path: HKCU\\Software\\E-G-O\\OsaKano. Original baked template value was also 100.'}, {'name': 'MusicVolume', 'value_dec': 100, 'scale_note': "BGM (wav via waveOut) volume, 0..100. ABSENT -> 0 -> no music. REG_DWORD = 100 (0x64) recommended for verification (game's own template default was 50; 100 = max audibility, user can lower in-game options)."}, {'name': 'VoiceVolume', 'value_dec': 100, 'scale_note': 'Voice (voice.lax via waveOut) volume, 0..100. ABSENT -> 0 -> no voice. REG_DWORD = 100 (0x64). Original baked template default was 100; runtime shadow had 50. Use 100 to confirm audibility.'}, {'name': 'SEVolume', 'value_dec': 100, 'scale_note': 'Sound-effects (common.lax via waveOut) volume, 0..100. ABSENT -> 0 -> no SE. REG_DWORD = 100 (0x64). Original baked template default was 100.'}]

